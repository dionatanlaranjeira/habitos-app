# ATENÇÃO: Arquivo gerado automaticamente. NÃO EDITE MANUALMENTE.
# Este arquivo armazena o estado atual do template para o 'make setup'.
OLD_PROJECT_NAME     := habitusapp
OLD_PACKAGE          := br.com.habitusapp
OLD_IOS_BUNDLE       := br.com.habitusapp
OLD_DISPLAY_NAME     := Habitus
OLD_IOS_DISPLAY_NAME := Habitus
